# Open-Meteo contract fixtures

Synthetic, deterministic fixtures modeled after the Forecast API response documented at
<https://open-meteo.com/en/docs>. They contain no customer coordinates and are used without
network access. `valid-24-points.json` covers the 23 provinces plus CABA fixture set.

